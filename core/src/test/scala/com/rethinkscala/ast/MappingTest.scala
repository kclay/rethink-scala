package com.rethinkscala.ast

import org.scalatest.FunSuite

import com.rethinkscala._
import ql2.{ Ql2 => p }
import com.rethinkscala.net.TableInfoResult

class MappingTest extends FunSuite with WithBase {

  ignore("should return TableResult") {
   /*
    assertAs[TableInfoResult](table.info, {
      i: TableInfoResult => i.name == tableName && i.kind == "TABLE" && i.db.name == "test"

    })*/

  }

}
